box: golang

build:

  steps:

    - setup-go-workspace

    - script:
        name: build
        code: |
          make build

    - script:
        name: run tests
        code: |
          make testacc

    - yieldr/test-reporter:
        id: $CODECLIMATE_REPO_TOKEN
