box: golang

build:

  steps:

    - setup-go-workspace

    - script:
        name: build
        code: |
          make build

    - script:
        name: run tests
        code: |
          make testacc OPTS=-coverprofile=c.out

    - yieldr/test-reporter:
        id: $CODECLIMATE_REPO_TOKEN
